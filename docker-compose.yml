version: '3'

services:
  app: 
    image: troddit
    build:
      dockerfile: Dockerfile
    env_file:
      - .env.local
    volumes:
      - codex-oauth:/var/lib/troddit-codex
    ports: 
      - 3000:3000

volumes:
  codex-oauth:
