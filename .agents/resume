#!/usr/bin/env bash
set -euo pipefail

# Dependencies and the toolchain are restored from the setup snapshot.
# No local database, authentication refresh, or reconnect step is required.
echo "Troddit ready; run yarn dev to start development."
